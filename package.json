{
  "name": "typespec-swagger-validator",
  "version": "1.0.0",
  "description": "Generate JS client libraries from Swagger and TypeSpec for comparison",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "tsv": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "tsx src/cli.ts",
    "generate": "tsx src/cli.ts generate --swagger examples/discriminator.json --typespec examples/discriminator.tsp --output output"
  },
  "keywords": [
    "typespec",
    "swagger",
    "autorest",
    "azure-sdk"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^25.2.0",
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
